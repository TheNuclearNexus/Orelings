name: Orelings 
version: '0.0.1'
minecraft: "1.20"

data_pack:
  name: "Orelings {{project_version}}"
  load:
    - "."
    - "data/common/modules": "../../libraries/common/modules"

resource_pack:
  name: "Orelings {{project_version}}"
  load:
    - "."

require:
 - remove

meta:
  common:
    namespace: "orelings"
    model_item: "minecraft:item/stone"

  remove:
   - mechanization

extend: ../../beet-common.yaml