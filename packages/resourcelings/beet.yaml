name: Resourcelings 
version: '0.0.1'
minecraft: "1.20"

data_pack:
  name: Resourcelings {{project_version}}
  load:
    - "."

resource_pack:
  name: "Resourcelings {{project_version}}"
  load:
    - "."

extend: ../../beet-common.yaml
